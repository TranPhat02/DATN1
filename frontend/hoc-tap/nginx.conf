server {
    # SPA: mọi route đều trả về index.html (React Router xử lý phía client)
    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html =404;
    }
}
